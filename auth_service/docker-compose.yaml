version: '3'

services:
  auth:
    build:
      context: .
      dockerfile: cmd/Dockerfile
    volumes:
      - db-data:/storage
    expose:
      - "32100"
    depends_on:
      - storage
    networks:
      - network
    environment:
      - CONFIG=/config/config.yaml

  storage:
    build:
      context: .
      dockerfile: ./storage/Dockerfile
    volumes:
      - db-data:/storage
    networks:
      - network

volumes:
  db-data:

networks:
  network:
    driver: bridge